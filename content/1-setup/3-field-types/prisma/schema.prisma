model Product {
  id          Int      @id @default(autoincrement())
  name        String
  description String   @db.Text
  price       Float
  quantity    Int
  inStock     Boolean
  releaseDate DateTime
  tags        Json
}
